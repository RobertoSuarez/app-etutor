import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#6528F7',
    },
    secondary: {
      main: '#F4D160',
    },
    background: {
      paper: '#FFFAF8',
    },
  },
});
