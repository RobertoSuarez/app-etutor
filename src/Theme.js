import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#1D5D9B',
    },
    secondary: {
      main: '#75C2F6',
    },
    background: {
      paper: '#F6F1F1',
    },
  },
});
