import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#146C94',
    },
    secondary: {
      main: '#19A7CE',
    },
    background: {
      paper: '#F6F1F1',
    },
  },
});
