import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#1D5D9B',
    },
    secondary: {
      main: '#F4D160',
    },
    background: {
      paper: '#F6F1F1',
    },
  },
});
